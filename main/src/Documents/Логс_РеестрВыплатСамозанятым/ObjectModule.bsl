
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	СуммаДокумента = Выплаты.Итог("СуммаСНалогом");
	
КонецПроцедуры

Процедура ПересчитатьНалогВСтроках() Экспорт
	Для Каждого Строка из Выплаты Цикл
		//Строка.СуммаСНалогом = ?(Строка.РассчитыватьНалог, Строка.Сумма/0.94, Строка.Сумма);
		Документы.Логс_РеестрВыплатСамозанятым.ПересчитатьНалогВСтроке(Строка);
	КонецЦикла;
КонецПроцедуры